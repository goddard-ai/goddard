- Model picker is now one filterable list with a jump rail instead of provider tabs: starred favorites lead (drag to reorder, ⌘⌥1–⌘⌥9 to apply), then recently used combos, then every provider's models with logos — each row enumerates a model/effort/tier selection with effort and fast mode shown on a second line. Rail buttons clear the filter and scroll to that section, and ⌘E / ⌘⇧E cycle reasoning effort for the current model in either direction
