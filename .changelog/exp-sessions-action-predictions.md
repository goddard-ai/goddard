- Action predictions: an Experiments-page opt-in that asks the evaluation model, at each turn's end, to guess your next move from a fixed set — follow-up prompts, Git actions, session management — while a local journal of consequential actions (~/.goddard/actions.jsonl) resolves each guess and logs verdicts to ~/.goddard/action-predictions.jsonl; nothing is suggested yet — shadow mode measures accuracy first
