- On macOS 26, the transparent sidebar renders as real Liquid Glass (`NSGlassEffectView`) lensing the desktop, tinted by the active theme; older macOS keeps the existing vibrancy path unchanged. The transparency slider now sweeps the full range — solid through frosted glass to an untinted lens at 100%. Context menus and dropdowns get a Liquid Glass treatment too — a translucent card with a specular top sheen over whatever lies beneath
