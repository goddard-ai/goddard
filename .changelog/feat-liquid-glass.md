- On macOS 26, the transparent sidebar renders as real Liquid Glass (`NSGlassEffectView`) lensing the desktop, tinted by the active theme; older macOS keeps the existing vibrancy path unchanged. The transparency slider now sweeps the full range — solid through frosted glass to a clear lens at 100%.
