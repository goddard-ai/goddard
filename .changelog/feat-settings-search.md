- Settings search now matches setting titles and descriptions across all pages: every section with a match renders in one scrollable list with the matching text highlighted, and the sidebar filters to those sections (clicking one scrolls to it). The field is focused as soon as Settings opens, matching a section's own title shows that whole section, and clicking a section or setting title jumps to it on its own page
