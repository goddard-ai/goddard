// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AgentPromptDelivery } from "./AgentPromptDelivery";
import type { AgentSession } from "./AgentSession";
import type { AgentWorkspace } from "./AgentWorkspace";
import type { AttachmentUpload } from "./AttachmentUpload";
import type { AutomationInput } from "./AutomationInput";
import type { ComposerDraftChange } from "./ComposerDraftChange";
import type { ComposerDrafts } from "./ComposerDrafts";
import type { CustomCommand } from "./CustomCommand";
import type { DaemonExposure } from "./DaemonExposure";
import type { DaemonSettings } from "./DaemonSettings";
import type { EvalQuestion } from "./EvalQuestion";
import type { EvalSettings } from "./EvalSettings";
import type { FriendSyncAlertAction } from "./FriendSyncAlertAction";
import type { GoalOperation } from "./GoalOperation";
import type { MessageAttachment } from "./MessageAttachment";
import type { Project } from "./Project";
import type { ProviderKind } from "./ProviderKind";
import type { ProviderResumeCursor } from "./ProviderResumeCursor";
import type { ProviderSessionForkRequest } from "./ProviderSessionForkRequest";
import type { RouteCandidate } from "./RouteCandidate";
import type { RouteTarget } from "./RouteTarget";
import type { SessionDetailTail } from "./SessionDetailTail";
import type { SessionMessageSearchScope } from "./SessionMessageSearchScope";
import type { UsageWindow } from "./UsageWindow";
import type { UserInputAnswer } from "./UserInputAnswer";
import type { UserInputQuestion } from "./UserInputQuestion";
import type { WireComputerToolRequest } from "./WireComputerToolRequest";
import type { WireDriverStartOptions } from "./WireDriverStartOptions";
import type { WireSessionOptions } from "./WireSessionOptions";
import type { WorkspaceOperation } from "./WorkspaceOperation";
import type { JsonValue } from "./serde_json/JsonValue";

export type Command = { "type": "attachSession" } | { "type": "start", options: WireDriverStartOptions, } | { "type": "prompt", prompt: string,
/**
 * The ids the submitting client already gave this turn and its user
 * message. The daemon republishes them with the submission so every
 * other client attached to the runtime mirrors the same rows instead
 * of minting its own; older clients omit them and the daemon mints.
 */
turnId?: string | null, messageId?: string | null,
/**
 * The prompt is provider-facing only — the internal nudge a
 * "continue" sends to an interrupted session. No client renders a
 * transcript row for it.
 */
hidden?: boolean,
/**
 * The composer's attachment chips. `prompt` already carries their
 * `@`-mention text; transports with a native attachment channel
 * (Copilot) also send them structurally.
 */
attachments?: Array<MessageAttachment>, } | { "type": "steer", prompt: string, } | { "type": "compact" } | { "type": "cancel" } | { "type": "cancelComputerUse" } | { "type": "refreshBackgroundWork" } | { "type": "stopBackgroundWork", key: JsonValue, controlId: string, } | { "type": "respond", requestId: string, optionId: string, } | { "type": "respondUserInput", requestId: string, answers: Array<UserInputAnswer>, } | { "type": "clarifyUserInput", requestId: string, content: string, } | { "type": "cancelUserInput", requestId: string, } | { "type": "goal", operation: GoalOperation, } | { "type": "claimManagedGoalTurn", goalId: string, turnId: string, } | { "type": "runComputerTool", request: WireComputerToolRequest, } | { "type": "rejectComputerTool", request: WireComputerToolRequest, reason: string, } | { "type": "applyOptions", options: WireSessionOptions, } | { "type": "rollback", turns: number, } | { "type": "fork", turnsToRemove: number, } | { "type": "getSettings" } | { "type": "updateSettings", settings: DaemonSettings, } | { "type": "getDaemonStats" } | { "type": "setDaemonExposure", exposure: DaemonExposure | null, } | { "type": "upsertCustomCommand", command: CustomCommand, } | { "type": "removeCustomCommand", id?: string | null, name?: string | null, } | { "type": "listCustomCommands" } | { "type": "probeProvider", provider: ProviderKind, binaryOverride: string | null, discoverModels: boolean, probeVersion: boolean, } | { "type": "sandboxSignIn", provider: ProviderKind, } | { "type": "sandboxAuthStatus", provider: ProviderKind, } | { "type": "fetchPlanUsage", provider: ProviderKind, binaryOverride: string | null, cliVersion: string | null, } | { "type": "consumeCodexResetCredit", redeemRequestId: string, } | { "type": "probeComputerPermissions", prompt: boolean, } | { "type": "loadUsageHistory", window: UsageWindow, projectRoots: Array<string>, } | { "type": "loadSkills", projects: Array<[string, string]>, } | { "type": "setSkillsEnabled", dirs: Array<string>, enabled: boolean, } | { "type": "trashSkills", dirs: Array<string>, } | { "type": "loadTaskState" } | { "type": "saveTaskState", projects: Array<Project>, liveSessionIds: Array<string>, sessions: Array<AgentSession>,
/**
 * Sessions whose `messages`/`transcript_blocks` carry only the tail
 * appended since the client's last save — spliced onto the daemon's
 * stored prefix rather than replacing it. Entries not listed here
 * are complete sessions as before.
 */
sessionTails: Array<SessionDetailTail>, } | { "type": "removeSession" } | { "type": "removeProject", projectId: string, } | { "type": "hydrateSession", sessionId: string, } | { "type": "searchSessionMessages", query: string, limit: number,
/**
 * Which sessions the search scans; absent means active tasks, so
 * pre-scope clients keep their palette behavior.
 */
scope: SessionMessageSearchScope, } | { "type": "listProviderSessions", provider: ProviderKind, limit: number, } | { "type": "loadProviderSession", cursor: ProviderResumeCursor, cwd: string, } | { "type": "evaluate", state: unknown, questions: { [key in string]: EvalQuestion },
/**
 * Which eval-driven feature made the call, recorded on the decision
 * log record. `None` — every caller before this field existed —
 * logs as a bare `"evaluate"`.
 */
feature?: string | null,
/**
 * Override for the call's latency budget in seconds. Latency-bound
 * callers (routing) leave it `None` for the default; long-context
 * callers such as provider-switch compaction pass a larger budget.
 */
timeoutSecs?: number | null, } | { "type": "testEvalConnection", settings: EvalSettings, } | { "type": "loadEvalUsage" } | { "type": "routeTask", prompt: string,
/**
 * Lightweight project context for the classifier — the project
 * name only; filesystem drilling is deliberately out of scope.
 */
project?: string | null, candidates: Array<RouteCandidate>, lastUsed?: RouteTarget | null, } | { "type": "recordRouteOverride", sessionId: string, target: RouteTarget, } | { "type": "listIntegrations" } | { "type": "connectIntegration", id: string, variantId: string, providers: Array<ProviderKind>, apiKey?: string | null, } | { "type": "setIntegrationProviders", id: string, providers: Array<ProviderKind>, } | { "type": "disconnectIntegration", id: string, } | { "type": "startIntegrationAuth", id: string, } | { "type": "loadComposerDrafts" } | { "type": "saveComposerDrafts", drafts: ComposerDrafts, generation: number, } | { "type": "applyComposerDraftChanges", changes: Array<ComposerDraftChange>, } | { "type": "storeBlob", mimeType: string, bytes: string, } | { "type": "importAttachment", name: string, upload: AttachmentUpload, } | { "type": "importPathAttachment", path: string, } | { "type": "readBlob", reference: string, } | { "type": "readAttachment", reference: string, path: string, } | { "type": "sweepBlobs" } | { "type": "forkSessionFromResponse", turnCount: number, } | { "type": "rewindSessionToMessage", turnCount: number, } | { "type": "forkProviderSession", request: ProviderSessionForkRequest, } | { "type": "workspace", operation: WorkspaceOperation, } | { "type": "openTerminal", cwd: string, cols: number, rows: number,
/**
 * The task whose surface opened this terminal, when one did. The
 * daemon sweeps the PTY when that task — or a workspace containing
 * `cwd` — is removed; `None` terminals leave only with their cwd's
 * workspace or an explicit `CloseTerminal`.
 */
owner?: string | null, } | { "type": "writeTerminal", data: string, } | { "type": "resizeTerminal", cols: number, rows: number, } | { "type": "closeTerminal" } | { "type": "closeSession" } | { "type": "agentCreateSession",
/**
 * Any provider Waku can drive. `None` inherits the sending task's
 * provider; the daemon rejects the command when no sending task is
 * known to inherit from.
 */
provider?: ProviderKind | null,
/**
 * An explicit provider model id, `"default"` (or empty) to select
 * the provider's own default model, or `None` to inherit the
 * sending task's model when it runs the resolved provider.
 */
model?: string | null,
/**
 * Absolute path of the project the task runs in. The daemon
 * resolves an existing project at that path, or registers a
 * primary Git checkout. Linked worktrees are never registered.
 */
project: string, workspace: AgentWorkspace,
/**
 * The ref the new worktree starts from. Required when `workspace`
 * is `worktree`, ignored for `local`.
 */
baseBranch?: string | null,
/**
 * The task's first prompt, delivered as a normal turn the moment
 * its session is running. There is no idle task creation path.
 */
prompt: string,
/**
 * Reasoning effort, service tier, and context window for the new
 * session. `None` inherits the sending task's value when it runs
 * the resolved provider and the resolved model's catalog still
 * lists it; `"default"` (or empty) selects the provider's own
 * default.
 */
reasoningEffort?: string | null, serviceTier?: string | null, contextWindow?: string | null, } | { "type": "agentPrompt",
/**
 * Waku task id. Exactly one of `task_id` and `thread_id` is
 * required.
 */
taskId?: string | null,
/**
 * Provider-native Agent CLI thread id, resolved against
 * daemon-known tasks. `provider` disambiguates when more than one
 * task carries the id.
 */
threadId?: string | null, provider?: ProviderKind | null, prompt: string, delivery: AgentPromptDelivery, } | { "type": "agentRenameSelf", title: string, } | { "type": "cancelQueuedPrompt", queuedMessageId: string, } | { "type": "getFriends" } | { "type": "sendFriendRequest", code: string, name: string, } | { "type": "respondFriendRequest", nodeId: string, accept: boolean, } | { "type": "withdrawFriendRequest", nodeId: string, } | { "type": "removeFriend", nodeId: string, } | { "type": "sendFileToFriend", nodeId: string, path: string, note: string | null, } | { "type": "sendMessageToFriend", nodeId: string, text: string, } | { "type": "cancelTransfer", transferId: string, } | { "type": "probeFriend", nodeId: string, } | { "type": "setFriendDisplayName", name: string, } | { "type": "setFriendNickname", nodeId: string, nickname: string | null, } | { "type": "getAutomations" } | { "type": "upsertAutomation", input: AutomationInput, } | { "type": "removeAutomation", automationId: string, } | { "type": "runAutomationNow", automationId: string, } | { "type": "agentReadSession",
/**
 * Waku task id. Exactly one of `task_id` and `thread_id` is
 * required for a foreign read.
 */
taskId?: string | null,
/**
 * Provider-native Agent CLI thread id, resolved against
 * daemon-known tasks. `provider` disambiguates when more than one
 * task carries the id.
 */
threadId?: string | null, provider?: ProviderKind | null,
/**
 * Restrict the answer to one turn's entries, by its 1-based turn
 * number — `items` each carry it, so a full read names the turn to
 * revisit.
 */
turn?: number | null, } | { "type": "agentSearchSessions", query: string, lastTurns?: number | null, } | { "type": "agentAsk", questions: Array<UserInputQuestion>, } | { "type": "shareProjectWithFriend", nodeId: string, projectPath: string, } | { "type": "unshareProjectWithFriend", nodeId: string, originUrl: string, } | { "type": "enableFriendSync", nodeId: string, originUrl: string, } | { "type": "disableFriendSync", linkId: string, } | { "type": "setFriendSyncConfig", linkId: string, autoPush: boolean, enabledBranches: Array<string>, } | { "type": "friendSyncNow", linkId: string, branch: string, } | { "type": "friendSyncAlertAction", alertId: string, action: FriendSyncAlertAction, } | { "type": "getFriendSyncBranches", linkId: string, } | { "type": "setFriendSessionSharing", nodeId: string, originUrl: string, enabled: boolean, } | { "type": "getFriendSessions", nodeId: string, originUrl: string, } | { "type": "watchFriendSession", nodeId: string, originUrl: string, sessionId: string, } | { "type": "unwatchFriendSession", sessionId: string, } | { "type": "getPairing" } | { "type": "respondPairRequest", requestId: string, accept: boolean, } | { "type": "revokePairedClient", clientId: string, };
