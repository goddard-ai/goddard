// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AutomationPrecheckResult } from "./AutomationPrecheckResult";
import type { AutomationRunStatus } from "./AutomationRunStatus";
import type { AutomationTrigger } from "./AutomationTrigger";

/**
 * One recorded execution attempt.
 */
export type AutomationRun = { id: string, automationId: string, trigger: AutomationTrigger, status: AutomationRunStatus,
/**
 * The occurrence this run covers — for a manual run, when it was
 * requested. Unix seconds.
 */
scheduledFor: number,
/**
 * When dispatch was attempted.
 */
startedAt: number | null, finishedAt: number | null,
/**
 * Task the run dispatched to, when one was created or reused.
 */
sessionId: string | null,
/**
 * Rejection detail for `Failed`/`Skipped*` runs.
 */
error: string | null, precheck: AutomationPrecheckResult | null,
/**
 * Groups identical repeated refusals: a run whose `refusal_key` matches
 * the previous run's folds into it, bumping this count, so an
 * always-unavailable schedule cannot flood history.
 */
refusalKey: string | null, refusalCount: number,
/**
 * Unix seconds.
 */
createdAt: number, updatedAt: number, };
