// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * A repo a friend shares with us.
 */
export type IncomingShareInfo = { peerId: string, peerName: string,
/**
 * Their project name — display only.
 */
projectName: string, originUrl: string,
/**
 * Our project with the same origin remote — `None` means we don't
 * have this repo, so there's nothing to sync.
 */
matchedPath: string | null, matchedProjectName: string | null,
/**
 * We already opted in — the `sync_links` row is authoritative.
 */
syncEnabled: boolean,
/**
 * The friend lets us watch this project's sessions — read-only, live.
 * `false` for shares offered before session sharing existed.
 */
shareSessions: boolean, receivedAtMs: number, };
