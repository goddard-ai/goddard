// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { FriendInfo } from "./FriendInfo";
import type { FriendRequestInfo } from "./FriendRequestInfo";
import type { IncomingShareInfo } from "./IncomingShareInfo";
import type { SharedProjectInfo } from "./SharedProjectInfo";
import type { SyncAlertInfo } from "./SyncAlertInfo";
import type { SyncLinkInfo } from "./SyncLinkInfo";
import type { TransferInfo } from "./TransferInfo";

/**
 * Whole friends document, carried wholesale on every change — it's small
 * and every client applies it, same convention as `SettingsChanged`.
 */
export type FriendsState = {
/**
 * This install's shareable code (`gfr-<endpoint id>`).
 */
friendCode: string,
/**
 * The name friends see on our requests and offers.
 */
displayName: string, friends: Array<FriendInfo>,
/**
 * Friend requests awaiting a local decision.
 */
incomingRequests: Array<FriendRequestInfo>,
/**
 * Requests we've sent that haven't been answered.
 */
outgoingRequests: Array<FriendRequestInfo>, transfers: Array<TransferInfo>,
/**
 * Repos we share with friends. `[]` matches older daemons.
 */
sharedProjects: Array<SharedProjectInfo>,
/**
 * Repos friends share with us, with daemon-side matching against
 * our projects already resolved.
 */
incomingShares: Array<IncomingShareInfo>,
/**
 * Sync relationships we opted into — one per (friend, origin).
 */
syncLinks: Array<SyncLinkInfo>,
/**
 * Sync decisions waiting on the user (conflicts, refused syncs).
 */
syncAlerts: Array<SyncAlertInfo>, };
