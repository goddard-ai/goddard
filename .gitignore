target
.goddard-cache
.waku-cache
.DS_Store
*.log
.env

/dist
node_modules
temp/

.DS_Store

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Session-local task contract — never committed.
/TASK.md
