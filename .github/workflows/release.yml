name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always
  RUSTC_WRAPPER: sccache
  SCCACHE_GHA_ENABLED: "true"

jobs:
  version:
    name: Resolve version
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      version: ${{ steps.resolve.outputs.version }}
      tag: ${{ steps.resolve.outputs.tag }}
      published: ${{ steps.published.outputs.published }}
    steps:
      - uses: actions/checkout@v6

      - name: Resolve version from Cargo.toml
        id: resolve
        run: |
          version=$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)
          if [ -z "$version" ]; then
            echo "::error::No version found in Cargo.toml"
            exit 1
          fi
          # Cargo.toml is the source of truth. A tag push must agree with it;
          # a manual run has no tag and simply releases what Cargo.toml says.
          if [ "$GITHUB_EVENT_NAME" = push ]; then
            tag="${GITHUB_REF_NAME#v}"
            if [ "$tag" != "$version" ]; then
              echo "::error::Tag ${GITHUB_REF_NAME} does not match Cargo.toml version ${version}"
              exit 1
            fi
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "tag=v$version" >> "$GITHUB_OUTPUT"
          echo "Releasing $version as v$version"

      # A published release for this version is already the finished product,
      # so rebuilding it would burn 90 minutes and could replace artifacts
      # people have downloaded. A leftover draft is this workflow's own
      # output, so it does not count and gets overwritten as before.
      - name: Look for an existing published release
        id: published
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.resolve.outputs.tag }}
        run: |
          draft=$(gh release view "$TAG" --repo "$GITHUB_REPOSITORY" \
            --json isDraft --jq .isDraft 2>/dev/null) || draft=missing
          if [ "$draft" = false ]; then
            echo "published=true" >> "$GITHUB_OUTPUT"
            echo "::notice::$TAG is already published; skipping this run"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

  linux-x86_64:
    name: Linux x86_64
    needs: version
    if: needs.version.outputs.published != 'true'
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    env:
      # mold via the clang driver — far faster than GNU ld. rust-lld is the
      # default on x86_64-linux since Rust 1.90, but not on aarch64, and mold
      # still beats both on a binary this size.
      RUSTFLAGS: "-C linker=clang -C link-arg=-fuse-ld=mold"
    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2

      - name: Install Rust
        run: |
          rustup toolchain install 1.96.0 --profile minimal
          rustup default 1.96.0

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes \
            clang \
            cmake \
            libfontconfig-dev \
            libvulkan1 \
            libwayland-dev \
            libx11-xcb-dev \
            libxi-dev \
            libxtst-dev \
            libxkbcommon-x11-dev \
            mold \
            pkg-config \
            xdg-desktop-portal

      # sccache caches per-crate compilation units, so it keeps hitting across
      # the version bump that rewrites Cargo.lock every release — a target/
      # cache would miss the exact key then cost multiple GiB of restore I/O.
      - name: Configure sccache
        uses: mozilla-actions/sccache-action@v0.0.11

      - name: Cache Cargo downloads
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ubuntu-22.04-rust-1.96.0-cargo-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            ubuntu-22.04-rust-1.96.0-cargo-

      - name: Bundle
        env:
          GODDARD_ANALYTICS_ENDPOINT: ${{ secrets.WAKU_ANALYTICS_ENDPOINT }}
          GODDARD_ANALYTICS_WEBSITE_ID: ${{ secrets.WAKU_ANALYTICS_WEBSITE_ID }}
        run: ./scripts/bundle-linux.sh

      # SSH remotes install the daemon alone; the app fetches this
      # version-matched tarball and verifies its checksum before installing.
      - name: Package goddard-daemon
        run: ./scripts/bundle-daemon.sh

      - uses: actions/upload-artifact@v7
        with:
          name: linux-x86_64
          # Named exactly, not globbed: `if-no-files-found: error` is then a
          # real check that this version built, rather than something an older
          # bundle can satisfy.
          path: |
            target/release/Goddard-${{ needs.version.outputs.version }}-x86_64-unknown-linux-gnu.tar.gz
            target/release/goddard-daemon-${{ needs.version.outputs.version }}-x86_64-unknown-linux-gnu.tar.gz
            target/release/goddard-daemon-${{ needs.version.outputs.version }}-x86_64-unknown-linux-gnu.tar.gz.sha256
          if-no-files-found: error

  linux-arm64:
    name: Linux arm64
    needs: version
    if: needs.version.outputs.published != 'true'
    runs-on: ubuntu-22.04-arm
    timeout-minutes: 90
    env:
      RUSTFLAGS: "-C linker=clang -C link-arg=-fuse-ld=mold"
    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2

      - name: Install Rust
        run: |
          rustup toolchain install 1.96.0 --profile minimal
          rustup default 1.96.0

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes \
            clang \
            cmake \
            libfontconfig-dev \
            libvulkan1 \
            libwayland-dev \
            libx11-xcb-dev \
            libxi-dev \
            libxtst-dev \
            libxkbcommon-x11-dev \
            mold \
            pkg-config \
            xdg-desktop-portal

      # sccache caches per-crate compilation units, so it keeps hitting across
      # the version bump that rewrites Cargo.lock every release — a target/
      # cache would miss the exact key then cost multiple GiB of restore I/O.
      - name: Configure sccache
        uses: mozilla-actions/sccache-action@v0.0.11

      - name: Cache Cargo downloads
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ubuntu-22.04-arm-rust-1.96.0-cargo-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            ubuntu-22.04-arm-rust-1.96.0-cargo-

      - name: Bundle
        env:
          GODDARD_ANALYTICS_ENDPOINT: ${{ secrets.WAKU_ANALYTICS_ENDPOINT }}
          GODDARD_ANALYTICS_WEBSITE_ID: ${{ secrets.WAKU_ANALYTICS_WEBSITE_ID }}
        run: ./scripts/bundle-linux.sh

      - name: Package goddard-daemon
        run: ./scripts/bundle-daemon.sh

      - uses: actions/upload-artifact@v7
        with:
          name: linux-arm64
          path: |
            target/release/Goddard-${{ needs.version.outputs.version }}-aarch64-unknown-linux-gnu.tar.gz
            target/release/goddard-daemon-${{ needs.version.outputs.version }}-aarch64-unknown-linux-gnu.tar.gz
            target/release/goddard-daemon-${{ needs.version.outputs.version }}-aarch64-unknown-linux-gnu.tar.gz.sha256
          if-no-files-found: error

  windows-x86_64:
    name: Windows x86_64
    needs: version
    if: needs.version.outputs.published != 'true'
    runs-on: windows-latest
    timeout-minutes: 90
    env:
      # rust-lld is a drop-in link.exe bundled in the toolchain — a large
      # single-codegen-unit binary takes link.exe minutes it doesn't take lld.
      RUSTFLAGS: "-C linker=rust-lld"
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        run: |
          rustup toolchain install 1.96.0 --profile minimal
          rustup default 1.96.0
          rustup target add x86_64-pc-windows-msvc

      - uses: oven-sh/setup-bun@v2

      - name: Install Inno Setup
        run: choco install innosetup --yes --no-progress

      - name: Configure sccache
        uses: mozilla-actions/sccache-action@v0.0.11

      - name: Cache Cargo downloads
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: windows-latest-rust-1.96.0-cargo-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            windows-latest-rust-1.96.0-cargo-

      - name: Bundle
        env:
          GODDARD_ANALYTICS_ENDPOINT: ${{ secrets.WAKU_ANALYTICS_ENDPOINT }}
          GODDARD_ANALYTICS_WEBSITE_ID: ${{ secrets.WAKU_ANALYTICS_WEBSITE_ID }}
          WINDOWS_CERTIFICATE: ${{ secrets.WINDOWS_CERTIFICATE }}
          WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
        run: bun scripts/bundle-windows.ts

      - uses: actions/upload-artifact@v7
        with:
          name: windows-x86_64
          path: |
            target/release/Goddard-${{ needs.version.outputs.version }}-x86_64-pc-windows-msvc.zip
            target/release/Goddard-${{ needs.version.outputs.version }}-x86_64-Setup.exe
          if-no-files-found: error

  windows-arm64:
    name: Windows arm64
    needs: version
    if: needs.version.outputs.published != 'true'
    runs-on: windows-11-arm
    timeout-minutes: 90
    env:
      RUSTFLAGS: "-C linker=rust-lld"
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        run: |
          rustup toolchain install 1.96.0 --profile minimal
          rustup default 1.96.0
          rustup target add aarch64-pc-windows-msvc

      - uses: oven-sh/setup-bun@v2

      - name: Install Inno Setup
        run: choco install innosetup --yes --no-progress

      - name: Configure sccache
        uses: mozilla-actions/sccache-action@v0.0.11

      - name: Cache Cargo downloads
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: windows-11-arm-rust-1.96.0-cargo-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            windows-11-arm-rust-1.96.0-cargo-

      - name: Bundle
        env:
          GODDARD_ANALYTICS_ENDPOINT: ${{ secrets.WAKU_ANALYTICS_ENDPOINT }}
          GODDARD_ANALYTICS_WEBSITE_ID: ${{ secrets.WAKU_ANALYTICS_WEBSITE_ID }}
          WINDOWS_CERTIFICATE: ${{ secrets.WINDOWS_CERTIFICATE }}
          WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
        run: bun scripts/bundle-windows.ts

      - uses: actions/upload-artifact@v7
        with:
          name: windows-arm64
          path: |
            target/release/Goddard-${{ needs.version.outputs.version }}-aarch64-pc-windows-msvc.zip
            target/release/Goddard-${{ needs.version.outputs.version }}-aarch64-Setup.exe
          if-no-files-found: error

  macos:
    name: macOS
    needs: version
    if: needs.version.outputs.published != 'true'
    runs-on: macos-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        run: |
          rustup toolchain install 1.96.0 --profile minimal
          rustup default 1.96.0

      - uses: oven-sh/setup-bun@v2

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Configure sccache
        uses: mozilla-actions/sccache-action@v0.0.11

      - name: Cache Cargo downloads
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: macos-latest-rust-1.96.0-cargo-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            macos-latest-rust-1.96.0-cargo-

      - name: Import Apple Developer Certificate
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        run: |
          KEYCHAIN_PASSWORD=$(openssl rand -base64 24)
          echo $APPLE_CERTIFICATE | base64 --decode > certificate.p12
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain
          security import certificate.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" build.keychain
          security find-identity -v -p codesigning build.keychain
          rm certificate.p12

      - name: Store notary credentials
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          xcrun notarytool store-credentials NOTARY \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_APP_SPECIFIC_PASSWORD" \
            --team-id "$APPLE_TEAM_ID"

      - name: Release
        env:
          GODDARD_SIGNING_IDENTITY: ${{ secrets.WAKU_SIGNING_IDENTITY }}
          GODDARD_ANALYTICS_ENDPOINT: ${{ secrets.WAKU_ANALYTICS_ENDPOINT }}
          GODDARD_ANALYTICS_WEBSITE_ID: ${{ secrets.WAKU_ANALYTICS_WEBSITE_ID }}
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        run: bun run release --local

      - name: Package goddard-daemon
        run: ./scripts/bundle-daemon.sh

      - uses: actions/upload-artifact@v7
        with:
          name: macos
          path: |
            dist/Goddard-${{ needs.version.outputs.version }}.dmg
            dist/Goddard-${{ needs.version.outputs.version }}.md
            dist/Goddard-${{ needs.version.outputs.version }}.zip
            dist/appcast.xml
            target/release/goddard-daemon-${{ needs.version.outputs.version }}-aarch64-apple-darwin.tar.gz
            target/release/goddard-daemon-${{ needs.version.outputs.version }}-aarch64-apple-darwin.tar.gz.sha256
          if-no-files-found: error

  draft-release:
    name: Draft GitHub release
    needs:
      [
        version,
        linux-x86_64,
        linux-arm64,
        windows-x86_64,
        windows-arm64,
        macos,
      ]
    if: needs.version.outputs.published != 'true'
    runs-on: ubuntu-latest
    # Per-asset retries can serialize a few minutes of backoff behind a
    # single hanging transfer.
    timeout-minutes: 25
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2

      - uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      # upload-artifact keeps each file's path below the common ancestor of
      # the upload's paths. Every job but macOS uploads only under
      # target/release/ and flattens cleanly; macOS spans dist/ and
      # target/release/, so its files download under those directories.
      # Flatten to the root the globs and appcast scripts below expect.
      - name: Flatten artifact directories
        run: find artifacts -mindepth 2 -type f -exec mv -f {} artifacts/ \;

      - name: Write the version pointers
        env:
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          # What `install.sh` and the site's Windows download page resolve
          # "latest" to. In-app updates read the appcasts instead.
          echo "$VERSION" > artifacts/latest-linux.txt
          echo "$VERSION" > artifacts/latest-windows.txt

      # Signed here rather than on platform runners: this is the only job that
      # holds every native artifact, and the signing key stays in one place.
      - name: Sign the Linux and Windows update feeds
        env:
          VERSION: ${{ needs.version.outputs.version }}
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        run: |
          # Merge into the live feeds so previously published releases keep
          # their entries; a first run simply starts from nothing.
          for platform in linux windows; do
            for arch in x86_64 aarch64; do
              curl -fsSL --max-time 30 \
                "https://releases.goddardai.org/appcast-$platform-$arch.xml" \
                -o "artifacts/appcast-$platform-$arch.xml" || true
            done
          done
          bun scripts/appcast-linux.ts artifacts "$VERSION"
          bun scripts/appcast-windows.ts artifacts "$VERSION"

      - name: Write release notes
        env:
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          bun -e '
            import { extractReleaseNotes } from "./scripts/changelog.ts";
            const version = process.env.VERSION;
            const file = Bun.file("CHANGELOG.md");
            const notes = (await file.exists())
              ? extractReleaseNotes(await file.text(), version)
              : null;
            // Mobile fragments fold into their own changelog so they stay
            // out of the desktop updater prompt; the release covers the
            // whole tag, so the body still carries them under ### Mobile.
            const mobileFile = Bun.file("CHANGELOG.mobile.md");
            const mobile = (await mobileFile.exists())
              ? extractReleaseNotes(await mobileFile.text(), version)
              : null;
            const mobileNotes = mobile
              ? `\n### Mobile\n\n${mobile.replace(/^### /gm, "#### ")}\n`
              : "";
            await Bun.write(
              "release-notes.md",
              `${notes ?? "See CHANGELOG.md for details."}\n${mobileNotes}`,
            );
          '

      # Downloads sit above the changelog. They link the R2 bucket rather than
      # GitHub assets, so they 404 until publishing the draft syncs them —
      # verifying them is part of the human review. The install.sh URL pins the
      # release tag so the script it fetches always matches this version.
      - name: Prepend download links
        env:
          VERSION: ${{ needs.version.outputs.version }}
          TAG: ${{ needs.version.outputs.tag }}
        run: |
          cat > downloads.md <<EOF
          ### Downloads

          - **macOS** — [Goddard-${VERSION}.dmg](https://releases.goddardai.org/Goddard-${VERSION}.dmg)
          - **Windows** — [x64 installer](https://releases.goddardai.org/Goddard-${VERSION}-x86_64-Setup.exe) · [arm64 installer](https://releases.goddardai.org/Goddard-${VERSION}-aarch64-Setup.exe) · [x64 zip](https://releases.goddardai.org/Goddard-${VERSION}-x86_64-pc-windows-msvc.zip) · [arm64 zip](https://releases.goddardai.org/Goddard-${VERSION}-aarch64-pc-windows-msvc.zip)
          - **Linux** — \`curl -fsSL https://raw.githubusercontent.com/goddard-ai/goddard/${TAG}/install.sh | sh\` · [x64 tarball](https://releases.goddardai.org/Goddard-${VERSION}-x86_64-unknown-linux-gnu.tar.gz) · [arm64 tarball](https://releases.goddardai.org/Goddard-${VERSION}-aarch64-unknown-linux-gnu.tar.gz)
          EOF
          cat downloads.md release-notes.md > notes.md
          mv notes.md release-notes.md

      - name: Draft the release
        uses: softprops/action-gh-release@v3
        with:
          draft: true
          # Prereleases (-beta.1 etc.) publish to GitHub only; sync-release
          # keeps them off the update feeds and latest-* pointers.
          prerelease: ${{ contains(needs.version.outputs.version, '-') }}
          # A manual run has no tag yet; publishing the draft creates this one
          # at the commit that was built.
          tag_name: ${{ needs.version.outputs.tag }}
          target_commitish: ${{ github.sha }}
          name: ${{ needs.version.outputs.tag }}
          body_path: release-notes.md

      # The action uploads every asset at once and never retries, so one
      # dropped connection or GitHub 500 fails the job with the other fifteen
      # assets already in place. `gh release upload` per file keeps the same
      # overwrite semantics via --clobber but retries only the transfer that
      # died.
      - name: Upload release assets
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.version.outputs.tag }}
        run: |
          shopt -s nullglob
          files=()
          for pattern in \
            'artifacts/*.tar.gz' \
            'artifacts/*.sha256' \
            'artifacts/*.dmg' \
            'artifacts/*.md' \
            'artifacts/*.zip' \
            'artifacts/*.exe' \
            'artifacts/appcast.xml' \
            'artifacts/appcast-linux-*.xml' \
            'artifacts/appcast-windows-*.xml' \
            'artifacts/latest-linux.txt' \
            'artifacts/latest-windows.txt'
          do
            # Intentionally unquoted: expand the glob like the action's
            # `files` list did, so an empty expansion can fail the job the
            # way `fail_on_unmatched_files` did.
            # shellcheck disable=SC2206
            matches=( $pattern )
            if (( ${#matches[@]} == 0 )); then
              echo "::error::No files matched: $pattern"
              exit 1
            fi
            files+=("${matches[@]}")
          done

          upload() {
            local file=$1
            for attempt in 1 2 3; do
              if gh release upload "$TAG" "$file" --repo "$GITHUB_REPOSITORY" --clobber; then
                return 0
              fi
              echo "::warning::Upload of $file failed (attempt $attempt/3)"
              sleep $((attempt * 15))
            done
            return 1
          }

          pids=()
          for file in "${files[@]}"; do
            upload "$file" &
            pids+=($!)
          done
          failed=0
          for pid in "${pids[@]}"; do
            wait "$pid" || failed=1
          done
          exit $failed
