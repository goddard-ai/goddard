{
  "name": "@goddard-ai/schema",
  "version": "0.1.0",
  "private": true,
  "license": "FSL-1.1-ALv2",
  "type": "module",
  "exports": {
    "./*": {
      "types": {
        "source": "./src/*.ts",
        "default": "./dist/*.d.mts"
      },
      "bun": "./src/*.ts",
      "import": "./dist/*.mjs"
    },
    "./json/*": "./json/*"
  },
  "scripts": {
    "prepare": "bun run scripts/generate-acp-adapters.js",
    "build": "tsdown --unused",
    "lint": "oxlint",
    "fmt": "prettier -w .",
    "typecheck": "tsgo --noEmit",
    "test": "bun test --dots"
  },
  "dependencies": {
    "acp-client": "catalog:",
    "@goddard-ai/ipc": "workspace:*",
    "rouzer": "catalog:",
    "zod": "catalog:"
  }
}
