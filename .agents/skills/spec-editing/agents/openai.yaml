interface:
  display_name: "Spec Editing"
  short_description: "Maintains capability-focused specs"
  default_prompt: "Use $spec-editing to update the capability contract for this product change."

policy:
  allow_implicit_invocation: true
