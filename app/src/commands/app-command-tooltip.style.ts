import { css } from "@goddard-ai/styled-system/css"

export default {
  content: css({
    display: "inline-flex",
    alignItems: "center",
    gap: "10px",
  }),
  shortcut: css({
    color: "muted",
  }),
}
